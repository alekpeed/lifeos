rules_version = '2';
// Deny client access until the LifeOS Firebase data model and migration are wired.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
